<div class="row">
	<!-- Main Content -->
	<div class="large-9 columns" role="content"><%= yield %></div>
	<!-- End Of Main Content -->
	<aside class="large-3 columns">

		<h5>Tags</h5>
		<ul class="side-nav">
			<% Tag.all.each do | t |%>
				<li> <%= link_to t.name , tag_path(t) %> </li>
			<% end %>
		</ul>

		<%= render :partial => "layouts/twitter_feed" %>
	</aside>
</div>
